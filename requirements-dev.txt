# Development dependencies for sbom-compile-order
# Install with: pip install -r requirements-dev.txt
# Or install the package in editable mode: pip install -e ".[dev]"

# Runtime dependencies
-r requirements.txt

# Development tools
pytest>=7.4.0
pylint>=3.0.0
black>=23.0.0

# Build tools
setuptools>=68.0
wheel
